from pyrobot.camera.v4l import V4LCamera
from pyrobot.vision.cvision import VisionSystem

def INIT(robot):
    return {"camera" : V4LCamera( 640, 480, channel = 0, 
                                  visionSystem = VisionSystem())}
